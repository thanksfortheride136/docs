---
title: 'Importing the Turtle Library'
description: '`turtle()` is a built-in Python library, meaning it can be importaed without having to install it through the terminal / Command Line Interface(CLI).'
---
## Syntax:

```python
from turtle import *
```
`from turtle import *` means: from the turtle library, import everyting (*). 
<Tip>Note that for many libraries and modules it is not good practice to import the entire library / module etc.</Tip>
<Warning>If you recieve any error mentioning tkinter or tk, please find documentation to install Tkinter here</Warning>


